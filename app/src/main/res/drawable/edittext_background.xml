<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">


    <!-- Bordes redondeados -->
    <corners android:radius="20dp" />

    <!-- Borde delgado oscuro -->
    <stroke
        android:width="1dp"
        android:color="@color/beige" />

    <!-- Relleno interno -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="10dp"
        android:bottom="10dp" />
</shape>
